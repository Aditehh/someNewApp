import React from 'react'
import { publishServiceAction } from '@/lib/actions/actions'

interface PublishProps {
  serviceId: number
}

export default function PublishServiceButton({ serviceId }: PublishProps) {
  return (
    <div>
      

    </div>
  )
}
